#ifndef BENCHMARK_PREFIX_H
#define BENCHMARK_PREFIX_H

#include "../src/prefix.h"

#include <chrono>
using std::chrono::milliseconds;
using std::chrono::system_clock;
using std::chrono::duration_cast;

#endif // BENCHMARK_PREFIX_H
